{
  "name": "resumehub",
  "version": "1.0.0",
  "description": "AI-powered resume analysis and job matching platform",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install:all && npm run build:client && npm run prisma:generate",
    "build:client": "npm run build --prefix client",
    "prisma:generate": "npx prisma generate --schema=server/prisma/schema.prisma",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
